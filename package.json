{
  "name": "qa-automation-project",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start:api": "json-server --watch mock-api/db.json --port 3001",
    "test": "npx playwright test",
    "test:ui": "npx playwright test --grep @ui",
    "test:api": "npx playwright test --grep @api",
    "show-report": "npx playwright show-report",
    "test:all": "npm run start:api & npx playwright test"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@playwright/test": "^1.56.1",
    "json-server": "^1.0.0-beta.3",
    "playwright": "^1.56.1"
  }
}
